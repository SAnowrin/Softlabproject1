export interface Test {
    name?: string,
    _id?: string,
    slug?: string,
    costPrice?: number,
    salePrice?: number,
    discountType?: number,
    discountAmount?: number,
    images?: string[],
}